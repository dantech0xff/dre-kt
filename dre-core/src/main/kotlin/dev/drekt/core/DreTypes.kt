package dev.drekt.core

/** Marker for state types in the DRE pattern. */
interface DreState

/** Marker for action types in the DRE pattern. */
interface DreAction

/** Marker for side effect types in the DRE pattern. */
interface DreEffect

/** Marker for async operation types in the DRE pattern. */
interface DreAsyncOp
